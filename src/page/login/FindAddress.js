import React, { useState } from 'react';
import {Form} from 'antd';




const Upload = () => {

    return (
        <Form>
            <Form>

            </Form>
        </Form>
    );
};

export default Upload;
